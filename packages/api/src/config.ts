import path from 'path';

// Path to the avatar uploads directory
export const uploadsDir = path.join(__dirname, '..', 'uploads', 'avatars');
export const submissionsDir = path.join(__dirname, '..', 'uploads', 'submissions');
